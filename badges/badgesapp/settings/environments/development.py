import logging

from badgesapp.settings.components.common import (
    INSTALLED_APPS,
    MIDDLEWARE,
)


DEBUG = True

ALLOWED_HOSTS = [
    'localhost',
    '127.0.0.1',
    '[::1]',
]

STATICFILES_DIRS = []

INSTALLED_APPS += ()
MIDDLEWARE += ()
